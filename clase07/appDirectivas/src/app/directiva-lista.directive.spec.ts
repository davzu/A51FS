import { DirectivaListaDirective } from './directiva-lista.directive';

describe('DirectivaListaDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectivaListaDirective();
    expect(directive).toBeTruthy();
  });
});
